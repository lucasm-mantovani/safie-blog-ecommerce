# Copie este arquivo para .env e preencha os valores reais
# NUNCA commite o arquivo .env no Git

# Apify — busca de notícias via Google News Scraper (opcional — o blog usa RSS por padrão)
APIFY_TOKEN=seu_token_aqui

# Anthropic — geração de artigos via Claude API
ANTHROPIC_API_KEY=sua_chave_aqui

# GitHub — push automático dos artigos gerados
GITHUB_REPO=lucasm-mantovani/safie-blog-ecommerce
GITHUB_TOKEN=seu_token_aqui
